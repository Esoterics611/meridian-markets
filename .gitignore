node_modules/
dist/
.env
.env.local
*.local
*.log
.DS_Store
.incremental/

# TypeScript build cache
*.tsbuildinfo

# OS / tool temp files
.msys*

# Claude harness
.claude/

# Captured L2 tapes (large, throwaway; mm-l2-session MM_L2_SAVE_TAPE / mm-l2-tune)
docs/research/l2-tapes/

# Per-run HL discovery scan outputs (reproducible; one reference copy stays committed)
docs/research/hl-universe/discovery-*.json
node_modules

# MM research captures (per-run, large; query with jq, never commit)
docs/research/flow-shadow-*.jsonl
logs/

# FIX-middleware hand-off summary (external session; not part of the repo's doc set)
docs/EXCHANGE_CONNECTIVITY.md

# per-book research run logs are evidence — re-include (top-level run-*.log stay ignored: per-run, large)
!docs/research/carry/*.log
!docs/research/outcome-rv/*.log
!docs/research/vrp/*.log

# Phase-0 maker tapes (orv-calibration.ts, ~50-100MB/day/market — query with jq/replay, never commit)
docs/research/orv-maker/tapes/
